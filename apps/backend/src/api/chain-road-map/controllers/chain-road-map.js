"use strict";

/**
 *  chain-road-map controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController("api::chain-road-map.chain-road-map");
